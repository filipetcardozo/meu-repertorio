import React from "react"
import styles from './lyrics.module.scss'

// MUI
import { Box } from "@mui/material"

export const LyricCard = () => {
    return <div className={styles.lyricCard}>
        Lyric Card
    </div>
}